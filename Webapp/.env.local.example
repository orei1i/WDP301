# Sao chép thành Webapp/.env.local rồi điền giá trị thật:  cp Webapp/.env.local.example Webapp/.env.local
# Bộ apiKey / authDomain / projectId / appId lấy ở Firebase Console → Project settings → General → Your apps → Web app.

NEXT_PUBLIC_API_URL=http://localhost:4000/api
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# tuỳ chọn: = SEED_PASSWORD của BE → nút đăng nhập demo một chạm ở /login
# NEXT_PUBLIC_DEMO_PASSWORD=
