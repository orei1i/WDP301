// Đã chuyển vào @ssm/shared để app mobile dùng chung đúng một bản.
// Giữ file này làm cầu nối: mọi `import { vnd, fmtDate } from '@/shared/lib/format'` chỉ cần một chỗ để đổi.
export * from '@ssm/shared';
