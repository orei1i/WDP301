// Đã chuyển vào @ssm/shared để app mobile dùng chung đúng một bản.
// Giữ file này làm cầu nối: mọi `import { CONTRACT_STATUS } from '@/shared/lib/labels'` chỉ cần một chỗ để đổi.
export * from '@ssm/shared';
