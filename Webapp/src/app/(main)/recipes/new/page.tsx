import type { Metadata } from "next";
import { ComingSoon } from "@/components/layout";

export const metadata: Metadata = { title: "Upload a recipe" };

export default function Page() {
  return <ComingSoon title="Upload a recipe" note="Multi-step recipe & video upload form — coming soon." />;
}
