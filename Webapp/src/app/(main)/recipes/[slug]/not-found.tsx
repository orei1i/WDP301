import { ComingSoon } from "@/components/layout";

export default function RecipeNotFound() {
  return <ComingSoon title="Recipe not found" note="It may have been removed or the link is wrong." />;
}
