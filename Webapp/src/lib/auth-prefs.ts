/**
 * Khoá localStorage cho các lựa chọn nhỏ liên quan đăng nhập.
 * Để ở lib chứ không export từ file page — Next.js chỉ chấp nhận một số export nhất định trong page.tsx.
 */
export const SKIP_SETPW = 'ssm_skip_setpw';
