// Đã chuyển vào @ssm/shared để app mobile dùng chung đúng một bản.
// Giữ file này làm cầu nối: mọi `import { CONTRACT_STATUS } from '@/lib/labels'` sẵn có không phải sửa.
export * from '@ssm/shared';
